# 2026-06-22

## export-ui-refactor

Refactored export UX on the Client Management page. Export button moved from the conditional export toolbar into the page header — always visible, disabled when nothing is selected. "Select all" button moved from the right-side toolbar into `<th class="select-col">`, directly above the checkbox column it controls. Export toolbar stripped to badge + count + Clear only.

**What changed:**
- `admin-clients.component.html` — export button in `.page-header` with `[disabled]` binding; select-all in `<th>`; export-action-btn removed from toolbar
- `admin-clients.component.css` — `.page-header` flex layout; `.header-export-btn` styles; `th.select-col` min-width for button
- `admin-clients.component.spec.ts` — 2 new tests (always-present, disabled→enabled); updated existing export-btn test
- `e2e/admin-client-export.spec.ts` — `toBeVisible()` → `toBeDisabled()` / `toBeEnabled()`

**Gotcha:** `openspec archive` requires delta spec MODIFIED headers to match the main spec exactly. "Export button is always visible…" was a new requirement (not MODIFIED) — archive aborted until corrected to `## ADDED Requirements`.

**Tests:** 27/29 unit (2 pre-existing unrelated), 6/6 e2e.

# Dev Log — 2026-06-18

## document-controller-idor-fix

**What shipped:** Applied the CRITICAL IDOR fix to `DocumentController` that was listed as fixed in the 2026-06-17 security scan but had not been implemented.

**Key changes:**
- `DocumentController` — all four endpoints (`upload`, `list`, `download`, `delete`) now call `clientService.findById(clientId, userId)` before delegating to `DocumentService`. Throws `ClientAccessDeniedException` → 403 if admin doesn't own the client.
- `list`, `download`, and `delete` endpoints now require `Authentication` parameter (previously took none).
- `DocumentControllerTest` — 4 new 403 ownership-violation tests; all 9 existing tests updated with auth + correct stubs; two `clientNotFound` tests fixed to stub `clientService.findById` (not the service layer).
- `DocumentControllerSecurityTest` — collateral `@MockitoBean ClientService` added for new constructor.

**Decisions:**
- Fix applied at controller layer, matching the `AdminMessageController` pattern applied yesterday.
- `resolveUserId` helper was already present in `DocumentController` — reused without change.

**Verification:** 16/16 tests pass (`DocumentControllerTest` 13 + `DocumentControllerSecurityTest` 3).

**Known gap:** No `clientNotFound` 404 tests for `download` and `delete` endpoints (low priority, no blocker).

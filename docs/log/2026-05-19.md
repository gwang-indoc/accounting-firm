# 2026-05-19

### 1. Client self-upload — backend

**Commits:** `13efcdd` → `62af02a` (Tasks 1–5 + review fixes)

**Feature:**

- Extract `FileUploadValidator` @Component; `DocumentService` and `MeDocumentService` both delegate to it. No behavior change for the existing staff-side upload.
- `MyDocumentsDto.Item` gains `uploadedByMe`; populated in `listMyDocuments` by comparing `client_document.uploaded_by` to the authenticated user's id.
- `DocumentNameConflictException` (409) and `PortalNotLinkedException` (403) wired into `GlobalExceptionHandler`. 409 body carries `{ message, filename, year }`.
- `MeDocumentService.uploadMyDocument`: resolve linked client → validate via `FileUploadValidator` → reject duplicate `(clientId, year, filename)` → save DB row → write file. DB-first ordering ensures `@Transactional` rollback on file-write failure.
- `POST /api/me/documents` on `MeDocumentController` returns 201 with the new `MyDocumentsDto.Item`.
- `LocalStorageService.relativePath()` extracted to eliminate path-string duplication between `DocumentService` and `MeDocumentService`.
- `MaxUploadSizeExceededException` → 413 (was 400 — fixed during review to match frontend expectations).

**Code Review Findings:**

| Severity | Issue | Fix |
|---|---|---|
| Critical | `uploadedAt` fallback (`LocalDateTime.now()`) could differ from DB value if JPA `@PrePersist` hadn't flushed | Set `uploadedAt` on entity before `save()` |
| Important | `MaxUploadSizeExceededException` → 400 but frontend expects 413 | Changed to `PAYLOAD_TOO_LARGE` |
| Important | Missing 413 controller test | Added `upload_returns413WhenFileTooLarge` |

**Tests:** 131 backend tests passing, 6 pre-existing context-load errors, 0 failures. 21 tests added in this slice.

**TDD Evidence:**

    FileUploadValidatorTest:  cannot find symbol class FileUploadValidator
    MeDocumentServiceTest:    method uploadedByMe() not found in MyDocumentsDto.Item
    GlobalExceptionHandlerTest: cannot find symbol DocumentNameConflictException
    MeDocumentServiceTest:    method uploadMyDocument(User, int, MultipartFile) not found
    MeDocumentControllerTest: status 405 (POST not mapped)

### 2. Client self-upload — frontend

**Commits:** `236fc53` → `d0eb8de` (Tasks 7–8 + review fixes)

**Feature:**

- `MyDocumentsService.upload(year, file)` posts `FormData` to `/api/me/documents?year=YYYY`. Returns `Observable<MyDocumentItem>`.
- `DocumentsComponent` extended with:
  - Upload button in the controls row (populated state); hidden `<input type="file" #fileInput>` triggered by it.
  - `onFileSelected`: reads `selectedYear() ?? emptyStateYear()` for the year, calls `upload`, appends the returned item immutably on success, shows status-specific snackbars on error.
  - Empty-state branch (linked + zero docs) gets a year picker (`empty-year-select`) + Upload button (`empty-upload-btn`).
  - "Uploaded by you" chip (`badge-you`) on `doc.uploadedByMe === true` rows.
  - Error messages mapped by HTTP status: 400/403/409/413/default.

**Code Review Findings:**

| Severity | Issue | Fix |
|---|---|---|
| Important | Unused `of`, `throwError`, `HttpErrorResponse` imports in spec | Removed |
| Important | File input missing `accept` attribute (UX) | Added `.pdf,.jpg,.jpeg,.png,.xlsx,.xls,.csv,.doc,.docx` |
| Minor | `emptyStateYearOptions` was `computed` with no signal deps | Converted to `readonly` IIFE property |
| Minor | Dead `selectedYear() == null` guard on upload button in populated state | Removed |

**Tests:** 94 frontend tests passing, 3 todo, 0 failures. 7 tests added in this slice.

**TDD Evidence:**

    DocumentsComponent spec: upload button test — `button.upload-btn` not found in DOM
    DocumentsComponent spec: badge-you chip test — `.badge-you` not found
    DocumentsComponent spec: onFileSelected not a function
    DocumentsComponent spec: uploading() not a function
    DocumentsComponent spec: empty-state year picker — `select.empty-year-select` not found
    MyDocumentsService spec: service.upload is not a function


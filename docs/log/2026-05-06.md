# Dev Log — 2026-05-06

## 1. Angular Material Install & Custom Theme (Group 1)

**Commits:** `847999a`, `042fe57`

**Feature:**

- Installed `@angular/material` and `@angular/cdk` (^21.2.9) via npm
- Added `provideAnimationsAsync()` to `app.config.ts` providers
- Defined custom Material theme in `src/styles.scss` using `mat.define-theme()`:
  - Primary palette: `mat.$azure-palette`, light mode
  - Dark navy toolbar: `mat-toolbar[color="primary"] { background: #0f172a }`
  - Body background: `#f1f5f9`
- Updated `angular.json` styles entry from `styles.css` → `styles.scss`
- Added `app.config.spec.ts` (GREEN: verifies `provideAnimationsAsync` is registered)
- Added `e2e/material-theme.spec.ts` (body background assertion; toolbar test skipped until Group 2)

**Code Review Findings:**

| Severity | Issue | Fix |
|---|---|---|
| Important | Selector `.mat-toolbar.mat-primary` relies on internal BEM class | Changed to `mat-toolbar[color="primary"]` attribute selector |
| Important | E2E toolbar test would error (no `mat-toolbar` in DOM yet) | Wrapped with `test.skip()` until Group 2 |
| Important | Dev log not created | Created this file |
| Minor | Orphaned `src/styles.css` still on disk | Deleted |
| Minor | `schematics` block empty — future `ng generate` would create `.css` | Set default to `scss` in `angular.json` |

**Tests:** 31 tests all passed, including 1 newly added test (`app.config.spec.ts`)

**TDD Evidence:**

    FAIL  src/app/app.config.spec.ts > appConfig > registers provideAnimationsAsync
    AssertionError: expected false to be true // Object.is equality
    Expected: true
    Received: false

_(RED failure before `provideAnimationsAsync()` was added to providers)_

---

## 2. NavbarComponent → MatToolbar + MatMenu (Group 2)

**Commits:** `d33e2fa`, `3a0670e`, `ffc6e0d`, `6ba12a4`, `9d7ae15`

**Feature:**

- Replaced `<nav class="navbar">` with `<mat-toolbar color="primary" class="navbar-toolbar">`
- Desktop nav links (Services, Security, Contact) now use `mat-button` directive
- "Book Consultation" CTA uses `mat-flat-button`
- Desktop "Client Login" replaced with `<button mat-button [matMenuTriggerFor]="loginMenu">` + `<mat-menu panelClass="login-menu-panel">` containing the Google OAuth2 sign-in panel
- Mobile drawer retains `<app-client-portal-login>` unchanged (Group 3 will replace with MatSidenav)
- MatMenu panel styles moved to global `styles.scss` (CDK overlay portal is outside component scope)
- `MatToolbar`, `MatButton`, `MatMenu`, `MatMenuItem`, `MatMenuTrigger` imported into NavbarComponent

**Code Review Findings:**

| Severity | Issue | Fix |
|---|---|---|
| Important | `data-testid="client-login-btn"` on both toolbar button and drawer component → DOM collision | Renamed drawer to `drawer-client-login-btn` |
| Important | Google sign-in `<a>` lacked `mat-menu-item` — no keyboard focus management | Added `mat-menu-item` attribute + `MatMenuItem` import |
| Important | CSS selector `.mat-toolbar.mat-primary` used; panel styles in component scope miss CDK overlay | Used `panelClass` + moved panel styles to `styles.scss` |
| Minor | Spurious blank line in `navbar.component.css` | Removed |
| Minor | No comment explaining why panel styles are in global scope | Added explanatory comment |

**Tests:** 35 tests all passed, including 4 newly added tests

**TDD Evidence:**

    FAIL  src/app/shared/navbar/navbar.component.spec.ts > NavbarComponent > renders a mat-toolbar element
    AssertionError: expected null not to be null
    Expected: not null
    Received: null

    FAIL  src/app/shared/navbar/navbar.component.spec.ts > NavbarComponent > Client Login button has matMenuTrigger and mat-menu is in template
    AssertionError: expected null not to be null
    Expected: not null
    Received: null

_(RED failures before mat-toolbar and mat-button implementations)_

---

## To Do

- [ ] Group 3: MatSidenav mobile drawer
- [ ] Group 4: DashboardComponent + HomeComponent + ContactComponent
- [ ] Group 5: E2E tests, verification & cleanup

# Dev Log — 2026-05-06

## 1. Angular Material Install & Custom Theme (Group 1)

**Commits:** `847999a`, `042fe57`

**Feature:**

- Installed `@angular/material` and `@angular/cdk` (^21.2.9) via npm
- Added `provideAnimationsAsync()` to `app.config.ts` providers
- Defined custom Material theme in `src/styles.scss` using `mat.define-theme()`:
  - Primary palette: `mat.$azure-palette`, light mode
  - Dark navy toolbar: `mat-toolbar[color="primary"] { background: #0f172a }`
  - Body background: `#f1f5f9`
- Updated `angular.json` styles entry from `styles.css` → `styles.scss`
- Added `app.config.spec.ts` (GREEN: verifies `provideAnimationsAsync` is registered)
- Added `e2e/material-theme.spec.ts` (body background assertion; toolbar test skipped until Group 2)

**Code Review Findings:**

| Severity | Issue | Fix |
|---|---|---|
| Important | Selector `.mat-toolbar.mat-primary` relies on internal BEM class | Changed to `mat-toolbar[color="primary"]` attribute selector |
| Important | E2E toolbar test would error (no `mat-toolbar` in DOM yet) | Wrapped with `test.skip()` until Group 2 |
| Important | Dev log not created | Created this file |
| Minor | Orphaned `src/styles.css` still on disk | Deleted |
| Minor | `schematics` block empty — future `ng generate` would create `.css` | Set default to `scss` in `angular.json` |

**Tests:** 31 tests all passed, including 1 newly added test (`app.config.spec.ts`)

**TDD Evidence:**

    FAIL  src/app/app.config.spec.ts > appConfig > registers provideAnimationsAsync
    AssertionError: expected false to be true // Object.is equality
    Expected: true
    Received: false

_(RED failure before `provideAnimationsAsync()` was added to providers)_

---

## 2. NavbarComponent → MatToolbar + MatMenu (Group 2)

**Commits:** `d33e2fa`, `3a0670e`, `ffc6e0d`, `6ba12a4`, `9d7ae15`

**Feature:**

- Replaced `<nav class="navbar">` with `<mat-toolbar color="primary" class="navbar-toolbar">`
- Desktop nav links (Services, Security, Contact) now use `mat-button` directive
- "Book Consultation" CTA uses `mat-flat-button`
- Desktop "Client Login" replaced with `<button mat-button [matMenuTriggerFor]="loginMenu">` + `<mat-menu panelClass="login-menu-panel">` containing the Google OAuth2 sign-in panel
- Mobile drawer retains `<app-client-portal-login>` unchanged (Group 3 will replace with MatSidenav)
- MatMenu panel styles moved to global `styles.scss` (CDK overlay portal is outside component scope)
- `MatToolbar`, `MatButton`, `MatMenu`, `MatMenuItem`, `MatMenuTrigger` imported into NavbarComponent

**Code Review Findings:**

| Severity | Issue | Fix |
|---|---|---|
| Important | `data-testid="client-login-btn"` on both toolbar button and drawer component → DOM collision | Renamed drawer to `drawer-client-login-btn` |
| Important | Google sign-in `<a>` lacked `mat-menu-item` — no keyboard focus management | Added `mat-menu-item` attribute + `MatMenuItem` import |
| Important | CSS selector `.mat-toolbar.mat-primary` used; panel styles in component scope miss CDK overlay | Used `panelClass` + moved panel styles to `styles.scss` |
| Minor | Spurious blank line in `navbar.component.css` | Removed |
| Minor | No comment explaining why panel styles are in global scope | Added explanatory comment |

**Tests:** 35 tests all passed, including 4 newly added tests

**TDD Evidence:**

    FAIL  src/app/shared/navbar/navbar.component.spec.ts > NavbarComponent > renders a mat-toolbar element
    AssertionError: expected null not to be null
    Expected: not null
    Received: null

    FAIL  src/app/shared/navbar/navbar.component.spec.ts > NavbarComponent > Client Login button has matMenuTrigger and mat-menu is in template
    AssertionError: expected null not to be null
    Expected: not null
    Received: null

_(RED failures before mat-toolbar and mat-button implementations)_

---

---

## 3. MatSidenav Mobile Drawer (Group 3)

**Commits:** `45d082b`, `91ca803`, `995a22f`, `4227e3a`

**Feature:**

- Replaced custom `@if (menuOpen())` mobile drawer in NavbarComponent with Angular Material `MatSidenav`
- `app.html` now wraps layout in `<mat-sidenav-container>` with `<mat-sidenav mode="over" position="start">`
- Sidenav contains `<mat-nav-list>` with: Services, Security, Client Login (inline expansion), Contact, Book Consultation (sky-blue)
- Language toggle pills at bottom of sidenav
- `NavbarComponent` receives `MatSidenav` via `@Input() sidenav!: MatSidenav` and calls `sidenav.toggle()` from hamburger
- `sidenavOpen` signal synced to real sidenav state via `openedChange` subscription (handles scrim-tap close)
- `(closed)` binding on `<mat-sidenav>` resets login expansion (`loginOpen`) when drawer closes
- Removed all `.mobile-drawer` CSS from `navbar.component.css`

**Code Review Findings:**

| Severity | Issue | Fix |
|---|---|---|
| Important | `sidenavOpen` desync when scrim closes drawer | Subscribe to `openedChange` in `ngOnInit`; use `takeUntilDestroyed` to prevent leak |
| Important | `openedChange` subscription not cleaned up | `takeUntilDestroyed(inject(DestroyRef))` |
| Suggestion | `loginOpen` not reset on close | `(closed)="loginOpen.set(false)"` binding |
| Minor | `height: 100%` on sidenav container unanchored | Added `height: 100%` to `html`, `body`, `app-root` |

**Tests:** 36 tests passed (2 todo), including 4 newly added tests

**TDD Evidence:**

    FAIL  src/app/app.spec.ts > App > renders mat-sidenav-container in app template
    AssertionError: expected null not to be null
    Expected: not null
    Received: null

    FAIL  src/app/shared/navbar/navbar.component.spec.ts > NavbarComponent > clicking hamburger calls sidenav.toggle()
    TypeError: Cannot read properties of undefined (reading 'toggle')

_(RED failures before app.html restructure and NavbarComponent sidenav @Input)_

---

## 4. DashboardComponent + HomeComponent + ContactComponent (Group 4)

**Commits:** `3b73cea`, `d1bbc76`, `a3e5a0c`, `7eef422`

**Feature:**

- DashboardComponent header replaced with `<mat-toolbar color="primary">`; logout button changed to `mat-stroked-button`; welcome block wrapped in `<mat-card>` with `<mat-card-header>` and `<mat-card-content>`
- HomeComponent services section wrapped in `<mat-card>`; hero uses `mat-headline-4`/`mat-body-1` typography classes; `<app-navbar />` removed (navbar now owned by root layout in `app.html`)
- ContactComponent heading/paragraph use `mat-headline-4`/`mat-body-1` typography classes; no component-level CSS needed
- Added `@include mat.typography-hierarchy($theme)` to `styles.scss` so `mat-headline-4` and `mat-body-1` selectors are active
- Removed redundant `standalone: true` from HomeComponent decorator (Angular 21 default)
- Replaced `RouterModule.forRoot([])` with `provideRouter([])` in HomeComponent spec (lighter setup)

**Code Review Findings:**

| Severity | Issue | Fix |
|---|---|---|
| Important | `mat-headline-4`/`mat-body-1` classes were inert — `mat.typography-hierarchy()` mixin never called | Added `@include mat.typography-hierarchy($theme)` to `styles.scss` |
| Minor | `standalone: true` redundant in Angular 21 HomeComponent | Removed |
| Minor | `RouterModule.forRoot([])` in HomeComponent spec is heavier than needed | Replaced with `provideRouter([])` |

**Tests:** 39 tests all passed, including 3 newly added tests (dashboard mat-toolbar, dashboard mat-card, home mat-card)

**TDD Evidence:**

    FAIL  src/app/features/client-portal/dashboard/dashboard.component.spec.ts > DashboardComponent > renders mat-toolbar with color primary
    AssertionError: expected null not to be null
    Expected: not null
    Received: null

    FAIL  src/app/features/home/home.component.spec.ts > HomeComponent > services section is wrapped in mat-card
    AssertionError: expected null not to be null
    Expected: not null
    Received: null

_(RED failures before DashboardComponent and HomeComponent Material refactor)_

---

## 5. E2E Tests, Verification & Cleanup (Group 5)

**Commits:** `d695b73`, `eb1ec59`

**Feature:**

- Updated `e2e/navbar.spec.ts`: replaced legacy selectors (`.navbar`, `.login-dropdown`, `.mobile-drawer`) with Angular Material selectors (`mat-toolbar`, `[data-testid="google-signin-link"]`, `.mat-drawer-backdrop`)
- Added `e2e/material-refactor.spec.ts`: 12 tests covering desktop toolbar, Client Login MatMenu open/close, Book Consultation CTA, and mobile MatSidenav open/close, inline Client Login expansion, scrim tap, sidenav CTA
- Un-skipped the mat-toolbar background colour test in `material-theme.spec.ts` (toolbar now live in DOM since Group 2)
- Fixed `styles.scss`: switched from `.login-menu-panel` to `.mat-mdc-menu-panel` — Angular Material 21's CDK Popover API overlay does not propagate `panelClass` to the rendered panel element
- Removed dead `panelClass="login-menu-panel"` attribute from `navbar.component.html`

**Code Review Findings:**

| Severity | Issue | Fix |
|---|---|---|
| Important | Duplicate MatMenu/MatSidenav coverage in both navbar.spec.ts and material-refactor.spec.ts | Removed 5 duplicate tests from navbar.spec.ts; canonical coverage in material-refactor.spec.ts |
| Important | Dead `panelClass="login-menu-panel"` attribute in navbar template (class never applied) | Removed the attribute |
| Suggestion | `.first()` missing on mat-toolbar locator in navbar.spec.ts | Applied `.first()` |

**Tests:** 19 Playwright E2E tests all passed · 39 Vitest unit tests all passed · 11 JUnit backend tests all passed

**TDD Evidence:**

Initial E2E run before selector fixes (first attempt after writing tests):

    6 failed
    ✘ Client Login MatMenu opens on click — Error: element(s) not found — locator('.login-menu-panel')
    ✘ pressing Escape closes Client Login MatMenu — same
    ✘ tapping scrim closes MatSidenav (mobile) — timeout 30s:
      <mat-drawer-inner-container> from <mat-sidenav> subtree intercepts pointer events

_(RED evidence: panelClass not propagated to DOM → switched to [data-testid="google-signin-link"]; sidenav width 280px intercepts backdrop center-click → used position {x:330, y:200})_

---

---

## 7. Client Registration — Group 1: Database & Entity

**Commits:** `7165917`, `0609b51`

**Feature:**

- Created `V2__make_google_sub_nullable.sql` — drops NOT NULL constraint on `users.google_sub`
- Created `V3__add_password_hash.sql` — adds `password_hash VARCHAR(60)` column
- Created `V4__widen_password_hash.sql` — widens `password_hash` to `VARCHAR(255)` (forward-safe for non-BCrypt algorithms)
- Updated `User` entity: `googleSub` now `nullable = true`; added `passwordHash` field with getter/setter
- Added two `UserEntityTest` tests: `savesUserWithNullGoogleSub()` and `savesUserWithPasswordHash()`

**Code Review Findings:**

| Severity | Issue | Fix |
|---|---|---|
| Important | `password_hash VARCHAR(60)` is zero-margin for future hash algorithms | Added V4 migration widening to VARCHAR(255) |
| Important | No test covered `password_hash` round-trip | Added `savesUserWithPasswordHash()` test |
| Minor | Redundant `nullable = true` on `@Column(name = "password_hash")` | Removed (default is nullable) |

**Tests:** 13 JUnit backend tests passing (2 newly added in this group)

**TDD Evidence:**

    [ERROR] com.gwhaitech.accountingfirm.auth.domain.UserEntityTest.savesUserWithNullGoogleSub -- Time elapsed: 0.007 s <<< ERROR!
    org.springframework.dao.DataIntegrityViolationException: not-null property references a null or transient value: com.gwhaitech.accountingfirm.auth.domain.User.googleSub
    Caused by: org.hibernate.PropertyValueException: not-null property references a null or transient value: com.gwhaitech.accountingfirm.auth.domain.User.googleSub
    [ERROR] Tests run: 2, Failures: 0, Errors: 1, Skipped: 0

---

## 8. Client Registration — Group 2: Backend Registration Endpoint

**Commits:** `c325a26`, `9c9ea5f`

**Feature:**

- Added `spring-boot-starter-validation` to `pom.xml`
- Created `RegisterRequest` DTO record with `@NotBlank`, `@Email`, `@Size(min=8)` constraints on all four fields (fullName, email, password, confirmPassword)
- Created `AuthService` with `register(RegisterRequest)`: null guard, password-match check (400), BCrypt hash, saves user with role=USER
- Added `EmailAlreadyRegisteredException` (custom typed exception); `AuthService.register()` catches `DataIntegrityViolationException` from `userRepository.save()` and rethrows it
- Added `POST /api/auth/register` to `AuthController` (201 on success); `@ExceptionHandler(EmailAlreadyRegisteredException)` returns 409
- Added `PasswordEncoder` (BCrypt) bean to `SecurityConfig`
- 3 new controller tests: 201/400/409 scenarios; `JwtAuthFilterTest` mock annotated with explanation comment

**Code Review Findings:**

| Severity | Issue | Fix |
|---|---|---|
| Important | `@ExceptionHandler(DataIntegrityViolationException)` too broad — masks non-email constraint failures | Created `EmailAlreadyRegisteredException`; `AuthService` catches and rethrows; controller maps typed exception to 409 |
| Important | `confirmPassword` missing `@Size(min=8)` — DTO inconsistency | Added `@Size(min=8)` to `confirmPassword` |
| Important | No null guard on `request.password()` before encode | Added `Objects.requireNonNull` guard |
| Minor | Wildcard import in `RegisterRequest.java` | Replaced with explicit imports |
| Minor | 201 test missing `verify(authService).register(any())` | Added `verify` call |
| Minor | Unexplained inert `AuthService` mock in `JwtAuthFilterTest` | Added explanatory comment |

**Tests:** 16 JUnit backend tests passing (3 newly added in this group)

**TDD Evidence:**

    [ERROR] com.gwhaitech.accountingfirm.auth.controller.AuthControllerTest.registerWithValidData_returns201 -- Time elapsed: 0.004 s <<< FAILURE!
    java.lang.AssertionError: Status expected:<201> but was:<404>

    [ERROR] AuthControllerTest.registerWithDuplicateEmail_returns409:146 Status expected:<409> but was:<404>
    [ERROR] AuthControllerTest.registerWithMismatchedPasswords_returns400:133 Status expected:<400> but was:<404>
    [INFO] Tests run: 6, Failures: 3, Errors: 0, Skipped: 0

_(RED failure before `POST /api/auth/register` endpoint was added — all three returned 404)_

---

## 9. Client Registration — Group 3: Backend Login Endpoint

**Commits:** `f404c8a`, `1b56b43`

**Feature:**

- Created `LoginRequest` DTO record: `@NotBlank @Email email`, `@NotBlank password`
- `AuthService.login()`: finds user by email (401 if not found), guards null `passwordHash` (Google-only accounts return 401), checks BCrypt match (401 if mismatch), returns `User`
- `AuthController POST /api/auth/login`: calls `authService.login()`, issues JWT via `jwtService.issueToken()`, sets httpOnly `SameSite=Strict` cookie via shared `buildJwtCookie()` helper
- `JwtService.expirationSeconds()` added so cookie `MaxAge` is derived from single source of truth (no duplicate `@Value` in controller)
- `buildJwtCookie()` private helper in `AuthController` removes cookie-attribute duplication between `login` and `logout`
- 4 new tests: 200+Set-Cookie on valid creds, 401 on wrong password, 401 on unknown email, 400 on blank email

**Code Review Findings:**

| Severity | Issue | Fix |
|---|---|---|
| Important | Cookie construction duplicated between `login` and `logout` | Extracted `buildJwtCookie()` private helper |
| Important | `app.jwt.expiration-ms` injected via `@Value` in controller AND read by `JwtService` | Added `JwtService.expirationSeconds()`; removed `@Value` from controller |
| Important | No test for 400 on invalid `LoginRequest` (blank email) | Added `loginWithBlankEmail_returns400` |

**Tests:** 20 JUnit backend tests passing (4 newly added in this group)

**TDD Evidence:**

    [ERROR] AuthControllerTest.loginWithUnknownEmail_returns401:188 Status expected:<401> but was:<404>
    [ERROR] AuthControllerTest.loginWithValidCredentials_returns200WithJwtCookie:163 Status expected:<200> but was:<404>
    [ERROR] AuthControllerTest.loginWithWrongPassword_returns401:176 Status expected:<401> but was:<404>
    [INFO] Tests run: 9, Failures: 3, Errors: 0, Skipped: 0

_(RED failures before `POST /api/auth/login` endpoint was added — all returned 404)_

---

## 10. Client Registration — Group 4: Frontend Services & Routes

**Commits:** `5550247`, `76764f9`, `d3e020d`, `fc9b089`

**Feature:**

- Navbar desktop "Client Login": replaced `<button [matMenuTriggerFor]="loginMenu">` with `<a mat-button routerLink="/login" data-testid="client-login-btn">Client Login</a>`; removed entire `<mat-menu #loginMenu>` block; removed `MatMenuModule` import from `navbar.component.ts`
- Mobile sidenav: "Client Login" `mat-list-item` updated to `routerLink="/login" (click)="sidenav.close()"`; `loginOpen` signal removed from `App`
- `AuthService`: added `register(dto)` → `POST /api/auth/register` → `Observable<void>`; added `loginWithEmail(dto)` → `POST /api/auth/login` → `Observable<void>`
- Stub components created: `LoginComponent`, `RegisterComponent`, `LoginEmailComponent` (all standalone, minimal templates)
- Routes added to `app.routes.ts`: `/login`, `/register`, `/login/email` as lazy-loaded routes

**Code Review Findings:**

| Severity | Issue | Fix |
|---|---|---|
| Minor | Empty `it.skip` in navbar spec represents deleted behavior (not planned future work) | Deleted the superseded skip |

**Tests:** 41 frontend tests passing | 3 todo (was 39 — net +2 new passing tests)

**TDD Evidence:**

Task 4.2 RED (navbar routerLink):
```
AssertionError: expected null to be '/login' // Object.is equality
- Expected: "/login"
+ Received: null
  at src/app/shared/navbar/navbar.component.spec.ts:161:45
```

Task 4.4 RED (AuthService.register):
```
TS2339: Property 'register' does not exist on type 'AuthService'.
  src/app/core/services/auth.service.spec.ts:47:12
```

---

## To Do

- [ ] client-registration Groups 5–6 remaining

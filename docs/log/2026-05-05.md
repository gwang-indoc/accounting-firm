# Dev Log — 2026-05-05

## 1. Backend Foundation

**Commit:** `0219025`

**Feature:**
- Fixed `groupId` from `com.indocsystems` to `com.gwhaitech` in `pom.xml`
- Added `jjwt-api`, `jjwt-impl`, `jjwt-jackson` (v0.12.6) dependencies
- Created `AccountingFirmApplication.java` main entry point
- Created `application.yml` (datasource, OAuth2, JWT config via env vars)
- Created `application-dev.yml` (local Postgres URL, CORS, cookie secure=false)
- Created Flyway migration `V1__create_users.sql` (users table)
- Created `User.java` entity mapped to `users` table
- Created `UserRepository.java` with `findByGoogleSub` and `findByEmail`

**Code Review Findings:**
| Severity | Issue | Fix |
|---|---|---|
| HIGH | Testcontainers JDBC URL in test `application.yml` leaked onto `spring-boot:run` classpath | Added `<useTestClasspath>false</useTestClasspath>` to spring-boot-maven-plugin |
| HIGH | `UserEntityTest` used Testcontainers which fails without Docker Desktop API access | Switched to `@TestPropertySource` pointing to local PostgreSQL |

**Tests:** 1 test passing (`UserEntityTest`)

---

## 2. JWT Service & Filter

**Commit:** `eec8a75`, `2d9cdd1`

**Feature:**
- Created `JwtConfig.java` binding `app.jwt.secret` and `app.jwt.expiration-ms`
- Created `JwtService.java` using jjwt: `issueToken`, `validateToken`, expiry handling
- Created `JwtAuthFilter.java` reading `jwt` cookie, validating via `JwtService`, setting `SecurityContextHolder`

**Code Review Findings:**
| Severity | Issue | Fix |
|---|---|---|
| HIGH | `JwtService` accepted null/short secret silently | Added constructor check: secret must be ≥ 32 bytes |

**Tests:** 3 new tests (JwtServiceTest × 3, JwtAuthFilterTest × 2) — 5 tests added

---

## 3. OAuth2 Success Handler & Auth Controller

**Commits:** `0e273f3`, `cb1e67c`

**Feature:**
- Created `OAuth2SuccessHandler.java`: find-or-create user by `google_sub`, issue JWT cookie, redirect to dashboard
- Created `UserDto.java` with `id`, `email`, `name`, `role`
- Created `AuthController.java`: `GET /api/auth/me` and `POST /api/auth/logout`
- Created `CorsConfig.java`: `CorsConfigurationSource` bean (required by Spring Security)
- Created `SecurityConfig.java`: OAuth2 login wiring, JWT filter, secure-by-default `anyRequest().denyAll()`

**Code Review Findings:**
| Severity | Issue | Fix |
|---|---|---|
| CRITICAL | `anyRequest().permitAll()` — fully open API | Replaced with explicit allowlist; `anyRequest().denyAll()` |
| HIGH | `CorsConfig` used `WebMvcConfigurer` — incompatible with Spring Security CORS | Rewrote as `@Bean CorsConfigurationSource` |
| HIGH | Logout cookie missing `Secure` and `SameSite=Strict` | Injected `@Value("${app.cookie.secure:true}")` and added attributes |

**Tests:** 2 new tests (OAuth2SuccessHandlerTest × 2, AuthControllerTest × 3) — 5 tests added

---

## 4. Angular Project Setup

**Commit:** `1624f30`

**Feature:**
- Scaffolded Angular 21 project (standalone, zoneless, Vitest)
- Added `proxy.conf.json` proxying `/api/**` to `http://localhost:8080`
- Set `environment.ts` `apiUrl`
- Created `CredentialsInterceptor` attaching `withCredentials: true` to all requests

**Code Review Findings:**
| Severity | Issue | Fix |
|---|---|---|
| HIGH | `proxy.conf.json` only covered `/api/**`; OAuth2 endpoints not proxied | Extended proxy to cover `/oauth2` and `/login/oauth2` |

**Tests:** 1 new test (CredentialsInterceptorSpec) — 1 test added

---

## 5. Auth Service & Guard

**Commit:** `46483d6`

**Feature:**
- Created `AuthService` with `currentUser = signal<UserDto | null>(null)` and `isAuthenticated = computed(...)`
- Registered `APP_INITIALIZER` calling `authService.loadCurrentUser()`
- Created `AuthGuard` (`CanActivateFn`) redirecting unauthenticated users to `/`
- Wired routes: `''` → `HomeComponent`, `'portal/dashboard'` → `DashboardComponent` (guarded)

**Tests:** 4 new tests (AuthServiceSpec × 2, AuthGuardSpec × 2) — 4 tests added

---

## 6. UI Components

**Commits:** `ed76428`, `4293dc7`, `ec61a58`, `441c00b`

**Feature:**
- Created `HomeComponent` with fixed navbar and hero/services placeholder sections
- Created `ClientPortalLoginComponent`: "Client Login" button + B2 dropdown with "Sign in with Google"
- Created `DashboardComponent`: displays user name, logout button
- Fixed Angular scaffold: replaced default `app.html` with `<router-outlet />`
- Fixed zoneless change detection: `provideZonelessChangeDetection()` (Angular 21.2)

**Code Review Findings:**
| Severity | Issue | Fix |
|---|---|---|
| HIGH | Login link `href="/api/auth/login"` — endpoint does not exist | Changed to `href="/oauth2/authorization/google"` |

**Tests:** 4 new tests (ClientPortalLoginComponentSpec × 4) — 4 tests added

---

## 7. E2E Verification & Bug Fixes

**Commit:** `549f254`

**Feature:**
- Ran full test suite: 11 backend tests, 10 frontend tests — all passing
- Confirmed no `System.out.println` or `console.log` in source
- Fixed all post-implementation runtime bugs (see below)
- Updated OpenSpec specs to reflect corrected OAuth2 URL and proxy requirements
- Archived `basic-project-structure` change to `openspec/changes/archive/`
- Promoted delta specs to `openspec/specs/`

**Code Review Findings:**
| Severity | Issue | Fix |
|---|---|---|
| HIGH | `spring-boot:run` picked up Testcontainers JDBC URL from test classpath | Added `<useTestClasspath>false</useTestClasspath>` to pom.xml |
| HIGH | OAuth2 login link sent browser to non-existent `/api/auth/login` | Fixed to `/oauth2/authorization/google`; extended proxy |

**Tests:** 11 backend + 10 frontend = 21 total, all passing

---

## landing-page-navbar — Group 1: NavbarComponent skeleton

**Commits:** `c09ec52`, `838b263`

**Feature:**

- Created `src/app/shared/navbar/navbar.component.{ts,html,css,spec.ts}`
- Standalone `NavbarComponent` with selector `app-navbar`
- Logo section: 42×42px dark rounded icon (css) with "税" character, "GWH Accounting" in white bold, tagline "Secure Tax & Accounting Portal" in `#38bdf8`
- Language toggle: `lang = signal<'en'|'zh'>('en')` — EN/中文 pill buttons switch the signal
- Fixed unused `CommonModule` import in follow-up commit

**Code Review Findings:**
| Severity | Issue | Fix |
|---|---|---|
| MEDIUM | `CommonModule` imported but unused in standalone component | Removed in 838b263 |
| (noted) | Active pill uses `#38bdf8` bg vs spec's "white on dark" | Accepted — matches visual design approved in brainstorming |
| (noted) | HomeComponent not yet wired to NavbarComponent | By design — planned for Group 5 |
| (noted) | No E2E test yet | By design — planned for Group 6 |

**Tests:** 6 new NavbarComponent tests, all passing (21 + 6 = 27 total)

---

## To Do

- [ ] landing-page-navbar Groups 2–6 (nav links, Client Portal, hamburger, HomeComponent wiring, E2E)
- [ ] Set a real `JWT_SECRET` (≥ 32 chars) in `.env` — current value is a placeholder

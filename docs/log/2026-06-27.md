# 2026-06-27

## client-workflow — shipped

Implemented the full client engagement workflow system across backend, frontend, and E2E tests.

### What was built

**Backend (Spring Boot)**
- V13 migration: added `business_type` (PERSONAL/CORPORATE/SELF_EMPLOYED) and `fiscal_year_end_month`/`fiscal_year_end_day` to `clients`. PERSONAL always stores Dec 31 server-side; CORPORATE/SELF_EMPLOYED require a valid calendar date validated with `MonthDay.of()`.
- V14 migration: `client_engagements` (UNIQUE on client_id + tax_year) and `client_engagement_history` (full audit trail with nullable `from_status`).
- Five-state machine — any-to-any transitions; no guard logic needed.
- Bilingual (EN/ZH) email notifications on IN_PROCESSING, PENDING_CLIENT_REVIEW, SUBMIT_TO_CRA, COMPLETED. Mail failure swallowed — status transition always succeeds.
- Cross-admin authorization enforced via `findClientForAdmin()` helper in `ClientEngagementService`. `listAll()` filters to calling admin's clients only.

**Frontend (Angular)**
- `AdminWorkflowComponent` at `/admin/workflow`: table with 6 columns, status + business-type `mat-select` filters using `computed()`.
- `AdminClientWorkflowComponent` at `/admin/clients/:id/workflow`: expandable engagement rows with lazy history fetch, "New Engagement" dialog, "Change Status" dialog with optional note.
- `AdminClientDialogComponent` updated: business type select + conditional FYE month/day inputs (hidden for PERSONAL).
- 5 Playwright E2E tests covering dashboard filters and full create → transition → history flow.

### Issues resolved

- **Security review** caught two HIGH findings before EVAL: (1) engagement endpoints didn't enforce cross-admin ownership, (2) `listAll()` exposed all admins' data. Both fixed in a single security pass.
- **Group 5 EVAL needed 2 attempts**: evaluator falsely claimed TestBed failed 0/9 (tests actually passed). Legitimate fixes applied: replaced nested subscribes with `switchMap+tap`, added `takeUntilDestroyed()` on all subscriptions, removed unused import.
- **`provideRouter([])` ordering**: must be placed before the custom `ActivatedRoute` mock or the router overrides the mock.

### Test counts
- Backend: 302 tests, 0 failures (5 pre-existing ApplicationContext errors unrelated to this change)
- Frontend: 9 new Vitest tests for client-workflow tab; 14 pre-existing file-level failures unchanged
- E2E: 5 new Playwright tests (3 dashboard, 2 client workflow)

## workflow-to-client-mgmt — archived

### Implementation (complete)

**Group 1: Backend activeEngagementStatus**
- Added `activeEngagementStatus: EngagementStatus | null` to `ClientDto` Java record.
- Added two Spring Data derived queries to `ClientEngagementRepository`: `findFirstByClientIdAndStatusNotOrderByTaxYearDesc` (primary) and `findFirstByClientIdOrderByTaxYearDesc` (fallback).
- Injected `ClientEngagementRepository` into `ClientService`; `findAll()` now computes active status per client.
- 4 new service tests + 2 new controller tests; all pass. Existing tests updated for new record field.

**Group 2: Frontend client list enhancements + navbar**
- Added `EngagementStatus` type and `activeEngagementStatus: EngagementStatus | null` field to `ClientDto` TypeScript interface.
- Added `workflowStateFilter = signal<string>('')` and `onWorkflowStateFilterChange()` to `AdminClientsComponent`; extended `filteredClients` computed to filter by workflow state with `'__none__'` sentinel for null-status clients.
- Added `openWorkflow(client)` method navigating to `/admin/clients/:id/workflow`.
- Updated template: Workflow State column, `<select data-testid="filter-workflow-state">` dropdown above table, Workflow action button per row.
- Removed Workflow nav link (`data-testid="admin-workflow-nav-link"`) from `navbar.component.html`.
- 7 new tests in `admin-clients.component.spec.ts` (column, filter, button); 1 new navbar test — all pass.

**Group 3: E2E + final verification**
- Backend: 308 tests, 0 failures, 5 pre-existing ApplicationContext errors (unchanged).
- Frontend: 163 tests passing (up from 155), 101 pre-existing failures unchanged.
- E2E: 5 new Playwright tests in `e2e/workflow-to-client-mgmt.spec.ts` — all pass.

### UI redesign (post-apply)

- `AdminClientWorkflowComponent` redesigned: dark card-per-engagement layout (`#1e293b` on `#0f172a`), pipeline-progress dots with lit connectors, semantic status badge via `[attr.data-status]` CSS attribute selectors, expandable history timeline.
- "New Engagement" dialog: replaced mat-form-field with a year stepper (±), dark surface via `panelClass: 'dark-dialog'` + global CSS in `styles.scss` (MDC variables — CDK overlay portals are outside component DOM, so component CSS can't reach them).
- "Change Status" dialog: status pills grid with live from→to preview, semantic color per status, native textarea for note.
- EN/ZH translations added for all 3 UIs (`public/i18n/admin/en.json` + `zh.json`, 21 new keys under `workflow.*`).
- `TranslateModule.forRoot()` added to `admin-client-workflow.component.spec.ts` TestBed (required when component uses `TranslateModule`).

### Archive
- Archived to `openspec/changes/archive/2026-06-27-workflow-to-client-mgmt/`.
- Canonical specs updated: `openspec/specs/admin-workflow-ui/spec.md` (1 modified), `openspec/specs/client-management/spec.md` (3 added, 1 modified).
- CLAUDE.md pitfall added: Angular Material dialogs need `panelClass` + global CSS on light themes — CDK overlay portals are outside the component DOM subtree.

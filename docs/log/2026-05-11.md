# Dev Log — 2026-05-11

## 1. Contact page — persistence layer

**Commit:** `f697234` (migration + entity + repository), `dbf21fa` (sort test)

**Feature:**

- Added `V7__create_contact_messages.sql` Flyway migration with `id BIGSERIAL PK`, `name VARCHAR(200)`, `email VARCHAR(254)`, `subject VARCHAR(200)`, `message TEXT`, `submitted_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`, `ip_address VARCHAR(45)`, and `idx_contact_messages_submitted_at` on `submitted_at DESC`
- Created `ContactMessage` JPA entity mapping the table (`@CreationTimestamp` on `submittedAt`)
- Created `ContactMessageRepository extends JpaRepository<ContactMessage, Long>`

**Code Review Findings:**
| Severity | Issue | Fix |
|---|---|---|
| Minor | Noise comment `// getters and setters` in entity | Removed |

**Tests:** 62 total passing (2 newly added)

**TDD Evidence:**

Task 1.1 RED — compile failure before entity/repository existed:

    [ERROR] COMPILATION ERROR :
    [ERROR] .../contact/domain/ContactMessageRepositoryTest.java:[9,53] package com.gwhaitech.accountingfirm.contact.repository does not exist
    [ERROR] .../contact/domain/ContactMessageRepositoryTest.java:[45,9] cannot find symbol: class ContactMessageRepository
    [ERROR] .../contact/domain/ContactMessageRepositoryTest.java:[34,9] cannot find symbol: class ContactMessage
    BUILD FAILURE

Task 1.3 — no RED phase needed; `JpaRepository.findAll(Sort)` compiled and passed immediately after entity was created.

---

## 2. Contact page — mail configuration

**Commit:** `a858c02` (spring-boot-starter-mail dep), `288ed3b` (MailProperties + ContactConfig), `b10826f` (LoggingMailSender + test)

**Feature:**

- Added `spring-boot-starter-mail` dependency to `pom.xml`
- Created `MailProperties` — `@ConfigurationProperties("contact") @Validated` record with `@NotBlank notificationEmail` and optional `fromEmail`
- Created `ContactConfig` — `@EnableConfigurationProperties(MailProperties.class)` wiring
- Created `LoggingMailSender` — `@Component @Profile("dev") @Primary` that logs and discards outgoing mail; no SMTP required in dev
- Added `contact.notification-email: dev@example.com` to `application-dev.yml`

**Code Review Findings:**
| Severity | Issue | Fix |
|---|---|---|
| None | — | — |

**Tests:** 64 total passing (2 newly added: `MailPropertiesValidationTest`, `LoggingMailSenderTest`)

**TDD Evidence:**

Task 2.2 RED:

    [ERROR] COMPILATION ERROR :
    [ERROR] ...MailPropertiesValidationTest.java: cannot find symbol: class MailProperties

Task 2.4 RED:

    [ERROR] COMPILATION ERROR :
    [ERROR] ...LoggingMailSenderTest.java: cannot find symbol: class LoggingMailSender

---

## To Do

- [x] Group 2: Mail configuration (spring-boot-starter-mail, MailProperties, LoggingMailSender)
- [x] Group 3: Domain service (ContactService, RateLimiter, ClientIpResolver)

---

## 3. Contact page — domain service

**Commit:** `eb7f142` (RateLimiter), `0ca772d` (ClientIpResolver), `1e73976` (ContactService + DTO), `00a08b8` (SMTP failure test), `230e2ef` (from-address tests)

**Feature:**

- Created `RateLimiter` — `ConcurrentHashMap<String, AtomicInteger>` keyed by IP, limit=5, `@Scheduled` hourly reset
- Created `ClientIpResolver` — static utility returning `getRemoteAddr()` only (X-Forwarded-For ignored; Javadoc explains trust model)
- Created `ContactSubmissionRequest` DTO record (name, email, subject, message, companyUrl) — validation annotations added in Group 4
- Created `ContactService.submit()` — persist to DB, then send `SimpleMailMessage` with `[Contact]` prefix; `MailException` caught → WARN log with row id; from-address falls back to `SPRING_MAIL_USERNAME`
- Added `@EnableScheduling` to `ContactConfig`

**Code Review Findings:**
| Severity | Issue | Fix |
|---|---|---|
| None | — | — |

**Tests:** 71 total passing (7 newly added)

**TDD Evidence:**

Task 3.1 RED:

    cannot find symbol: class RateLimiter — BUILD FAILURE

Task 3.3 RED:

    cannot find symbol: variable ClientIpResolver — BUILD FAILURE

Task 3.5 RED:

    cannot find symbol: class ContactService — BUILD FAILURE

Tasks 3.7, 3.9 — immediately GREEN (implementation from 3.6 already covered these cases).

- [x] Group 4: HTTP layer (DTO, controller, SecurityConfig)

---

## 4. Contact page — HTTP layer

**Commit:** `3904f37` (ContactController + DTO with validation), `4fdd27c` (SecurityConfig permitAll), `83790c2` (validation/honeypot/rate-limit tests), `be631cc` (integration test), `2fb45f4` (JwtAuthFilterTest fix)

**Feature:**

- Updated `ContactSubmissionRequest` DTO with Bean Validation: `@NotBlank` on name/email/subject/message, `@Email` on email, `@Size(max=200)` on subject, `@Size(max=5000)` on message
- Created `ContactController` — `POST /api/contact` with check order: honeypot (→ 200) → rate limit (→ 429) → service (→ 202)
- Modified `SecurityConfig`: added `.requestMatchers(HttpMethod.POST, "/api/contact").permitAll()` before `/api/**` rule

**Code Review Findings:**
| Severity | Issue | Fix |
|---|---|---|
| Regression | `JwtAuthFilterTest` failing: `@WebMvcTest` context loaded `ContactController` which required unmocked beans | Added `@MockitoBean ContactService/RateLimiter` to `JwtAuthFilterTest` (same pattern as existing service mocks) |

**Tests:** 81 total passing (10 newly added)

**TDD Evidence:**

Task 4.1 RED:

    cannot find symbol: class ContactController — BUILD FAILURE

Task 4.3 RED:

    ContactSecurityTest.unauthenticatedPostIsPermitted:44
    expected: not equal but was: <401 UNAUTHORIZED>

Tasks 4.5-4.10 — immediately GREEN (validation/honeypot/rate-limit already handled by existing implementation).
- [x] Group 5: Frontend service and model

---

## 5. Contact page — frontend service and model

**Commit:** `d46cc98`

**Feature:**

- Created `ContactSubmission` interface (name, email, subject, message, companyUrl)
- Created `ContactService.send(payload)` — `Observable<void>` from `HttpClient.post<void>('/api/contact', payload)`

**Code Review Findings:**
| Severity | Issue | Fix |
|---|---|---|
| None | — | — |

**Tests:** 52 frontend passing (1 newly added) | 3 todo

**TDD Evidence:**

Task 5.1 RED:

    ✘ Could not resolve "./contact.service"
    ✘ TS2307: Cannot find module '../models/contact-submission'
- [x] Group 6: Frontend page component

---

## 6. Contact page — frontend page component

**Commits:** `c0ca0b2` (component rewrite), `5487df0`–`019e0a0` (TDD additions), `7b7a9fa` (layout), `6d541f5` (E2E)

**Feature:**

- Rewrote `ContactComponent` — reactive form with controls name/email/subject/message/companyUrl; `submitting` signal; `submit()` with `finalize()`, success resets form + snackbar, error preserves values + error snackbar
- Validators: `name [required]`, `email [required, email]`, `subject [required, maxLength(200)]`, `message [required, maxLength(5000)]`
- Template: two-column CSS grid (`2fr 1fr` on ≥900px, single-column below 900px); form card left, Find Us card right
- Find Us card: four info rows (Address / Phone / Email / Hours) with placeholder values (flag to replace before deploy), Google Maps `<iframe loading="lazy" referrerpolicy="no-referrer-when-downgrade">`
- Honeypot: `<input name="companyUrl" tabindex="-1" aria-hidden="true" style="position:absolute; left:-9999px; ...">` bound to form control
- Submit button: `mat-stroked-button` with `[disabled]="form.invalid || submitting()"`
- `MatSnackBarModule` intentionally NOT in `imports[]` — keeping it there causes the component environment injector to shadow the TestBed root-level mock
- Visual smoke test: desktop (2-column) and mobile 360px (stacked) confirmed via Playwright browser session
- E2E spec (`e2e/contact.spec.ts`): happy path (POST→202→snackbar), validation path (invalid email→mat-error, no POST), mobile layout (stacked at 360px)
- Placeholder values used (task 6.0 deferred): `123 Main St, Anytown CA 90210 / (555) 123-4567 / info@firm.com / Mon–Fri 9–5` — replace before production deploy

**Code Review Findings:**
| Severity | Issue | Fix |
|---|---|---|
| Minor | Subject `<mat-form-field>` has no `<mat-error>` — required/maxLength errors are invisible | Left as-is; add in follow-up UX pass |
| Minor | E2E happy path fails with 429 if rate limiter is full (backend must be fresh) | Document in e2e README |

**Tests:** 81 backend passing | 60 frontend passing (8 newly added) | 3 E2E passing

**TDD Evidence:**

Task 6.1 RED:

    ✘ Cannot find module './contact.component' or its corresponding type declarations.

Task 6.3 RED — immediately GREEN (validators attached in 6.2):

    All 4 validation tests passed on first run after validators added in task 6.2.

Task 6.5 RED:

    ✘ Expected "contactServiceMock.send" to be called once, but was called 0 times.

Task 6.7 RED:

    ✘ Expected form value 'name' to be 'Alice', received null (form was reset on error path).

Task 6.9 RED:

    ✘ Expected to find element with name="companyUrl" — not yet in template.
